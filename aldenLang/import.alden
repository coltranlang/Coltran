get User from "./aldenLang/module" # You don't need to specify the file extension, it will be added automatically.
println(User.Greet())

get Http from "http"

object Header
    Content_Type: "application/json"
end

let http = Http.create("https://www.aldenlang.org", Header, "This is a body")
println(http.dd)