# User module test
get User from "./aldenLang/module" # You don't need to specify the file extension, it will be added automatically.\
println(User.Greet())

# Http module test
get Http from "http"

object Header
    Content_Type: "application/json"
end

let http = Http.new("https://www.aldenlang.org", Header, "This is a body")
#println(http.post(), http.get())
# Math module test
get Math from "math"
println(Math.run())