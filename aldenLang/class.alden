# class constructors must start with _ or $ . class is still a work in progress
class Person(_age, _name)
    def getName()
       println("Your name is " + _name)
    end
    def getAge()
       println("I am " + str(_age) + " years old")
    end
end



let person = Person(20, "John")
print(person.getName())
