# class constructors must start with _ or $ . class is still a work in progress
class Person(age, name)
    def getName()
       println("Your name is " + name)
    end
    def getAge()
       println("Your are " + str(age) + " years old")
    end
end



let person = Person(20, "John")
println(person.getName(), person.getAge())